<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="net.simpledev.leo.chishona">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Categories">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Numbers"
            android:label="@string/category_numbers" />
        <activity
            android:name=".FamilyMembers"
            android:label="@string/category_family" />
        <activity
            android:name=".WeekDays"
            android:label="@string/category_weekdays" />
        <activity
            android:name=".Phrases"
            android:label="@string/category_phrases" />
        <activity
            android:name=".Proverbs"
            android:label="Proverbs" />
        <activity
            android:name=".Vowels"
            android:label="Vowels And Usages/ Combination" />
        <activity android:name=".Animals" />
        <activity android:name=".Months" />
        <activity android:name=".Trees"></activity>
    </application>

</manifest>